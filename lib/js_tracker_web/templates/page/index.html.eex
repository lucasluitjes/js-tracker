<h2>JS Tracker</h2>
<ul>
  <%= for target <- @targets do %>
    <li>
      <%= target.url %>
      <span><%= link "Show recordings", to: recording_path(@conn, :index, target_id: target.id), class: "btn btn-default btn-xs" %></span>
    </li>
  <% end %>
</ul>
<%= paginate @conn, @kerosene %>